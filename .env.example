# Server-side API credentials
TOUR_API_KEY=replace_with_tour_api_key
NAVER_MAPS_CLIENT_ID=replace_with_naver_maps_client_id
NAVER_MAPS_CLIENT_SECRET=replace_with_server_only_naver_maps_client_secret
ODSAY_API_KEY=replace_with_odsay_api_key
# Optional: 발급 시 무료 openrouteservice를 우선 사용하며, 미설정 시 OSM 공개 보행 라우터를 사용합니다.
OPENROUTESERVICE_API_KEY=replace_with_free_openrouteservice_api_key
# 무료 2,000회/일 한도 아래의 애플리케이션 안전 상한(최대 1,900)
OPENROUTESERVICE_DAILY_LIMIT=1900

# Multi-provider identity verification (PortOne V2 + KG Inicis unified identity)
# Store/channel IDs are returned to the browser. Keep the API secret server-only.
PORTONE_STORE_ID=store-replace_with_portone_store_id
PORTONE_IDENTITY_CHANNEL_KEY=channel-key-replace_with_unified_identity_channel_key
PORTONE_API_SECRET=replace_with_server_only_portone_v2_api_secret

# Optional request/cache tuning

# Apple Developer / APNs (실제 값과 .p8 파일은 저장소에 커밋하지 않음)
APPLE_TEAM_ID=replace_with_10_character_team_id
APPLE_APP_ID_PREFIX=replace_with_10_character_app_id_prefix
APPLE_BUNDLE_ID=com.wizide.gachi
APNS_KEY_ID=replace_with_10_character_key_id
APNS_PRIVATE_KEY_PATH=/secure/path/AuthKey_replace_with_key_id.p8
APNS_ENVIRONMENT=production
APNS_BATCH_SIZE=25
APNS_POLL_INTERVAL_MS=2000
